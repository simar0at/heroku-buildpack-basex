#!/usr/bin/env bash
# bin/release <build-dir>

cat << EOF
addons: []
default_process_types:
  web: /app/.heroku/basex/bin/set-basex-admin-password /app 2; /app/.heroku/basex/bin/basexhttp -h "\$PORT"
EOF
