#!/usr/bin/env bash
# copy-basex-data-if-empty <build-dir>

BUILD_DIR=${1:-}

if [ -z "$(ls -A $BUILD_DIR/.heroku/basex/data)" ]
then
  cp -ra $BUILD_DIR/.heroku/basex/data.image $BUILD_DIR/.heroku/basex/data
fi